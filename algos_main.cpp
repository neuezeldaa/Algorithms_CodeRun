#include "algos_header.h"
#include "algos_main.h"



int main()
{
    // 1 cout << "The middle element in the list: " << SredniyElement(1, 6, 3) << endl;
    
    // 2 cout << "Count of cristmas toys on the tree: " << NaryajElka(3) << endl;
    
    // 3 cout << "Min. Counts of shots to kill all flying birds: " << AngryPigs(3) << endl;

    // 4 cout << "The key [i] doesn't work? " << Keybord() <<endl;
   
    // 5 cout << "Count of different numbers in the list: " << Different_numbers() << endl;
   
    // 6 cout << "Exercise about poliglotes" << poliglott() << endl;

    // 7 cout << "Colors of kubiki:" << kubiki(3, 4) << endl;

    // 8 cout << "Count of added numbers: " << OpenCalculator(1, 2, 3, 456789) << endl;

    // 9 cout << "Maximum of profit: " << AlotOfChairs(2, 3) << endl;

    // 10 cout << "Probability of error on each server: " << Error(2) << endl;

    // 11 cout << "Intersection of two vectors: " << intersecc() << endl;
    
    // 12 cout << "Realisation of binary search function: " << check_vectors() << endl;
    
    // 13 cout << "closest element in N-list for j: " << closer_to_the_item() << endl;
   
    // 14 cout << "is the list increased?: " << is_increase() << endl;

    // 15 cout << "Middle element of summa of pair of vectors: " << Mediana(3, 6) << endl;

    // 16 cout << "Transpose of the matrix multiplication of matrices A and B:" << MultiplyAndTransponiruy(1, 2, 3) << endl;

    // 17 cout << "Summa of numbers in the rectangle:" << pref_summ(3,3,3) << endl;

    // 18 cout << "Max Lenght of the wire for K segment is: " << otrezki(4, 11) << endl;

    // 19 cout << "Size of of the room: " << plitka_445(8, 1) << endl;

    // 20 cout << "is the size of brick compare to the hole? " << Uznik_74(1, 1, 1, 1, 1) << endl;

    // 21 cout << "Enter commands to interaction with the stack: " << Stack_140() << endl;

    // 22 cout << "How many times does the current word appearse? " << Counter_Of_Appearance_51() << endl;

    return 0;
}


